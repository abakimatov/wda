import styled from 'styled-components'
import { STempButton } from './css'
import { Button } from '@material-ui/core'

export const STemplateButton = styled(Button)`
  cursor: pointer;
  height: auto;
  ${STempButton};
`
export const SSaveButton = styled(Button)`
  width: 100%;
  border: none;
  outline: none;
  text-transform: none;
  background: linear-gradient(180deg, #ffffff 0%, #eeeeee 100%);
  border: 1px solid #c8c8c8;
  box-sizing: border-box;
  box-shadow: 0px 1px 0px rgba(0, 0, 0, 0.18);
  border-radius: 4px;
`
export const SСancelButton = styled(Button)`
  width: 100%;
  border: none;
  outline: none;
  text-transform: none;
  background: linear-gradient(180deg, #ffffff 0%, #eeeeee 100%);
  border: 1px solid #c8c8c8;
  box-sizing: border-box;
  box-shadow: 0px 1px 0px rgba(0, 0, 0, 0.18);
  border-radius: 4px;
`
export const SDrawerSimleDataButton = styled(Button)`
  width: 100%;
  border: none;
  outline: none;
  text-transform: none;
  background: linear-gradient(180deg, #ffffff 0%, #eeeeee 100%);
  border: 1px solid #c8c8c8;
  box-sizing: border-box;
  box-shadow: 0px 1px 0px rgba(0, 0, 0, 0.18);
  border-radius: 4px;
`
